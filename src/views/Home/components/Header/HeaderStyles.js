import { styled } from "@mui/material/styles";

import GridWrapper from '../../../../components/wrappers/GridWrapper';

export const RootHeaderContainer = styled( GridWrapper )(() => ({
  margin: '25px 25px',
}));
